# Kari Command Usage

After installing Kari, you can use the following commands:

```bash
kari
```

#### Commands
```bash
kari <command> [options]
start - Start a local network
move - MoveVM
keytool - Kari keystore tool
version, --V - Show version
```